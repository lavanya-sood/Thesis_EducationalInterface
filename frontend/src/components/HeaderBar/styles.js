import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
    appBar: {
        display: 'flex',
        justifyContent: 'space-between',
    },
    welcomeInfo: {
        color: 'white',
    },
    headingappBar: {
        flexGrow: 1,
    }
}));